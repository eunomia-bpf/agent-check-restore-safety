# Validation-section revision outline

Role: extend the existing validation section from finite executable checks and
Lean mechanization to the accepted fixed Codex adapter evidence without
changing the paper's theorem claims.

Paragraph moves:

1. Keep the bounded Python and Lean evidence unchanged in meaning.
2. Add one compact runtime-instantiation paragraph: real/pinned App Server,
   deterministic model fixture, adapter-owned lifecycle, isolated sink, and
   worker-only fault boundary.
3. Add one compact policy table reporting observed decisions, unsafe accepts,
   safe rejects, and truncation.
4. Add one correctness/observability paragraph with the 89/89, 20/20, 44,
   33, 187, and 3/3/0 gates.
5. End with the non-overclaim boundary: no native Restore/Merge, product-wide
   mediation, topology-activation crash, frontend death, arbitrary tools, or
   general concrete-refinement proof.

Page strategy: first add the evidence faithfully, compile, then cut redundant
finite-validation narration rather than shrinking fonts or moving required
content into an unread appendix.  References and AI acknowledgment remain
outside the 12-page body allowance.
