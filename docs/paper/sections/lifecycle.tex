\section{One Agent, Two Histories}
\label{sec:lifecycle}

Consider a procurement Agent authorized to place one order.  It durably
reserves the budget, giving receipt log \(\Delta\) with
\(\mathsf{lab}(\Delta)=r\), and saves checkpoint \(k\) before approval.
Approval then commits as cell \(a\), so
\(\mathsf{lab}(\Delta)=ra\).  The Agent requests a choice Fork between two
	suppliers.  Its protected completion contract has exactly two indexed
	outcomes.
\begin{equation}
\label{eq:running-outcomes}
\begin{split}
 o_A&:\ p_A\prec s_A,\\
 o_B&:\ p_B\prec s_B ,
\end{split}
\end{equation}
where \(p\) and \(s\) denote payment and shipment occurrences.  The
alternatives are exclusive, but both must have a safe completion at the cut.

Logical occurrence is not effect identity.  The protected-call registry
authenticates
\[
 q_{\rm cell}(s_A)=q_{\rm cell}(s_B)=s.
\]
Both branches copied the same shipment handle, while payments retain
distinct cells \(p_A\) and \(p_B\).  A later Restore of \(k\) will create a
new approval occurrence \(a'\) with \(q_{\rm cell}(a')=a\).  The authority policy
allows prefixes of \(rap_As\) and \(rap_Bs\), but no trace containing both
payments or a payment before \(a\).

\paragraph{The admission calculation}
Using the admission notation formalized in \cref{sec:admission}, write \(M_o\)
for outcome \(o\)'s policy-safe resolved completions.  At this cut each outcome
has one causal linearization, resolving to
\[
\begin{aligned}
 z_A&=\Fresh(p_A,p_A)\Fresh(s_A,s),\\
 z_B&=\Fresh(p_B,p_B)\Fresh(s_B,s).
\end{aligned}
\]
Thus \(M_{o_A}=\{z_A\}\), \(M_{o_B}=\{z_B\}\), and
\(\widehat B^\dagger=\{(o_A,z_A),(o_B,z_B)\}\), with \(W^\dagger\) their
prefix closure.  On acceptance, these prefixes become the generated behavior
of the compiled monitor.  If policy excludes \(rap_Bs\), then
\(M_{o_B}=\varnothing\), and the Fork is rejected because pruning supplier
\(B\) would change its meaning.  \Cref{sec:admission} gives the shared-prefix
case where every outcome starts safe but no prefix-robust family exists.

\begin{figure*}[t]
  \centering
  \begin{tikzpicture}[
      font=\scriptsize,
      box/.style={draw,rounded corners=1pt,minimum height=5.5mm,
        align=center,inner sep=2pt},
      node/.style={draw,circle,minimum size=5.4mm,inner sep=0pt},
      arr/.style={-{Latex[length=1.5mm]},thick},
      stale/.style={-{Latex[length=1.5mm]},thick,dashed},
      lab/.style={font=\scriptsize\bfseries,anchor=east}
    ]
    \node[lab] at (0,2.15) {logical history};
    \node[node] (h0) at (0.65,2.15) {$h_0$};
    \node[node] (al) at (1.85,2.15) {$a$};
    \node[box] (f) at (3.1,2.15) {choice\\Fork};
    \node[node] (ba) at (4.45,2.62) {$A$};
    \node[node] (bb) at (4.45,1.68) {$B$};
    \node[box] (ms) at (5.95,2.62) {select\\Merge};
    \node[node] (ha) at (7.25,2.62) {$h_A$};
    \node[box] (rl) at (8.35,1.68) {live\\Restore $k$};
    \node[node] (cl) at (9.75,1.68) {$a'$};
    \node[box] (jn) at (11.15,2.15) {join\\Merge};
    \draw[arr] (h0) -- node[above] {$k$} (al);
    \draw[arr] (al) -- (f);
    \draw[arr] (f) -- (ba);
    \draw[arr] (f) -- (bb);
    \draw[arr] (ba) -- (ms);
    \draw[arr] (ms) -- (ha);
    \draw[stale] (h0.north) -- (0.65,3.18) --
      node[above] {checkpoint clone: new occurrence, same cell}
      (8.35,3.18) -- (rl.north);
    \draw[arr] (rl) -- (cl);
    \draw[arr] (ha) -- (jn);
    \draw[arr] (cl) -- (jn);
    \draw[stale] (bb) to[bend left=17]
      node[below] {must lose authority} (ms);

    \node[lab] at (0,0.65) {world receipts};
    \node[node] (r) at (0.65,0.65) {$r$};
    \node[node] (a) at (3.05,0.65) {$a$};
    \node[node] (pa) at (6.05,0.65) {$p_A$};
    \node[node] (s) at (10.2,0.65) {$s$};
    \draw[arr] (r) -- node[above] {append only} (a);
    \draw[arr] (a) -- (pa);
    \draw[arr] (pa) -- (s);
    \node[align=center] at (9.75,1.12)
      {\(\Alias(a',a)\)\\no new receipt};

    \node[lab] at (0,-0.75) {policy-domain epoch};
    \node[box] (e0) at (0.85,-0.75) {$\eta_0$};
    \node[box] (ef) at (3.7,-0.75) {$\eta_F:\ A,B$};
    \node[box] (ea) at (6.8,-0.75) {$\eta_A$};
    \node[box] (ej) at (11.15,-0.75) {$\eta_J$};
    \draw[arr] (e0) -- node[above] {atomic swap} (ef);
    \draw[arr] (ef) -- node[above] {atomic swap} (ea);
    \draw[arr] (ea) -- node[above] {atomic swap} (ej);
  \end{tikzpicture}
  \caption{An Agent has an editable logical history, a non-editable world
  history, and one current policy-domain epoch.  Restore clones logical occurrences
  but preserves authenticated cell identity.  A history cut must therefore
  order both receipt-changing \Fresh events and receipt-silent \Alias events
  against a policy-domain epoch swap.}
  \label{fig:two-histories}
\end{figure*}

\paragraph{The fresh race}
After the shared approval and before either payment, the Agent requests
\(\mathsf{MergeSelect}(g,A,\sigma)\).  If an old supplier-\(B\) protected use
commits \(p_B\) first, that occurrence durably selects \(B\) and changes the receipt
prefix from \(ra\) to \(rap_B\).  Selecting \(A\) must therefore be re-derived
and rejected.  If the Merge cut wins, it closes the current
policy-domain epoch and rebinds every live gate before any later \(p_B\) can
commit.  Installing a new program while an old policy-domain epoch remains
active permits both payments.

\paragraph{The alias race}
Now consider the live Restore in \cref{fig:two-histories}.  Cloning \(k\)
creates a fresh logical approval occurrence \(a'\) and gate, but retains
cell \(a\).  Because \(a\) is already receipted, executing \(a'\) is an
\Alias that advances the restored continuation past approval while leaving
\(\Delta\) unchanged.  A concurrent join or replacement compiled before that
progress is stale even though its receipt comparison still succeeds.  The cut
must therefore compare a logical-occurrence frontier \(\chi\) in addition to
the receipt frontier.

The example exposes the whole problem.  Workspace equality cannot recover
which branch relation is live, and receipt equality cannot detect alias
progress.  Cell equality cannot determine the required obligation projection,
while a caller-supplied target automaton cannot certify any of these facts.
\Cref{sec:model} instead derives the target through a trusted history judgment.
