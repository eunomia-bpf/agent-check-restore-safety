# Legacy ACRFence paper

This directory contains the earlier ACRFence / semantic rollback paper and its
ACM-specific build assets. It is retained for history and is not the current
submission source.

The current anonymous IEEE CSF paper is:

```text
docs/paper/main.tex
```

To build this legacy paper independently, run `make` in this directory.
